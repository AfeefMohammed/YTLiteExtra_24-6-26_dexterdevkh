name: Create YouTube Plus App By Version

on:
  workflow_dispatch:
    inputs:
      enable_youpip:
        description: "Integrate YouPiP"
        type: boolean
        required: true
        default: true

      enable_yq:
        description: "Integrate YouQuality"
        type: boolean
        required: true
        default: true

      enable_youslider:
        description: "Integrate YouSlider"
        type: boolean
        required: true
        default: true

      enable_ytflags:
        description: "Integrate YTFlags"
        type: boolean
        required: true
        default: true

      enable_youspeed:
        description: "Integrate YouSpeed"
        type: boolean
        required: true
        default: true

      enable_ygc:
        description: "Integrate YouGetCaption"
        type: boolean
        required: true
        default: true

      enable_ryd:
        description: "Integrate Return YouTube Dislikes"
        type: boolean
        required: true
        default: true

      enable_ytabc:
        description: "Integrate YTABConfig"
        type: boolean
        required: true
        default: true

      enable_demc:
        description: "Integrate DontEatMyContent"
        type: boolean
        required: true
        default: true

      enable_ycq:
        description: "Integrate YouChooseQuality (Keep disabled if YouQuality is on)"
        type: boolean
        required: true
        default: false

      enable_ytuhd:
        description: "Integrate YTUHD (Disabled - known to crash modern YT)"
        type: boolean
        required: true
        default: false

      enable_vbyt:
        description: "Integrate VolumeBoostYT (Disabled - known to crash modern YT)"
        type: boolean
        required: true
        default: false

      enable_ytamp:
        description: "Integrate YTAnotherMiniplayer (Disabled - known to crash modern YT)"
        type: boolean
        required: true
        default: false

      ipa_url:
        description: "URL to the decrypted IPA file"
        default: ""
        required: true
        type: string

      tweak_version:
        description: "YTLite version (e.g. 5.2b4)"
        default: "5.2b4"
        required: true
        type: string

      display_name:
        description: "App Name"
        default: "YouTube"
        required: true
        type: string

      bundle_id:
        description: "BundleID"
        default: "com.google.ios.youtube"
        required: true
        type: string

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build-tweaks:
    uses: ./.github/workflows/_build_tweaks.yml
    with:
      enable_youpip: ${{ inputs.enable_youpip }}
      enable_yq: ${{ inputs.enable_yq }}
      enable_youslider: ${{ inputs.enable_youslider }}
      enable_ytflags: ${{ inputs.enable_ytflags }}
      enable_youspeed: ${{ inputs.enable_youspeed }}
      enable_ygc: ${{ inputs.enable_ygc }}
      enable_ryd: ${{ inputs.enable_ryd }}
      enable_ytabc: ${{ inputs.enable_ytabc }}
      enable_demc: ${{ inputs.enable_demc }}
      enable_ycq: ${{ inputs.enable_ycq }}
      enable_ytuhd: ${{ inputs.enable_ytuhd }}
      enable_vbyt: ${{ inputs.enable_vbyt }}
      enable_ytamp: ${{ inputs.enable_ytamp }}
      tweak_version: ${{ inputs.tweak_version }}

  package:
    name: Package IPA
    runs-on: macos-latest
    needs: build-tweaks
    permissions:
      contents: write

    steps:
      - name: Download built debs
        uses: actions/download-artifact@v4
        with:
          name: built-debs
          path: ${{ github.workspace }}

      - name: Hide sensitive inputs
        uses: levibostian/action-hide-sensitive-inputs@v1
        with:
          exclude_inputs: bundle_id,display_name,enable_demc,enable_ryd,enable_ytabc,enable_youpip,enable_yq,enable_youslider,enable_ytflags,enable_youspeed,enable_ygc,tweak_version

      - name: Download and validate IPA
        run: |
          wget "${{ github.event.inputs.ipa_url }}" --no-verbose -O youtube.ipa
          file_type=$(file --mime-type -b youtube.ipa)
          if [[ "$file_type" != "application/x-ios-app" && "$file_type" != "application/zip" ]]; then
            echo "::error::Validation failed: The downloaded file is not a valid IPA. Detected type: $file_type."
            exit 1
          fi

      - name: Get YouTube Version and Build Date
        run: |
          unzip -q youtube.ipa -d ytextracted
          echo "YT_VERSION=$(grep -A 1 '<key>CFBundleVersion</key>' ytextracted/Payload/YouTube.app/Info.plist | grep '<string>' | awk -F'[><]' '{print $3}')" >> $GITHUB_ENV
          echo "BUILD_DATE=$(date +'%d %b %y' | tr '[:upper:]' '[:lower:]')" >> $GITHUB_ENV

      - name: Install cyan
        run: pipx install --force https://github.com/asdfzxcvbn/pyzule-rw/archive/main.zip

      - name: Inject tweaks into IPA
        run: |
          tweaks="ytplus.deb OpenYoutubeSafariExtension.appex"
          for f in *.deb; do
            [ -f "$f" ] && tweaks="$tweaks $f"
          done
          cyan -i youtube.ipa -o "YouTubeLitePlusExtra_${{ env.YT_VERSION }}_v ${{ github.event.inputs.tweak_version }}_${{ env.BUILD_DATE }}.ipa" -uwef $tweaks -n "${{ github.event.inputs.display_name }}" -b ${{ github.event.inputs.bundle_id }}

      - name: Upload to GitHub Releases
        uses: softprops/action-gh-release@v3
        with:
          tag_name: ytp-${{ env.YT_VERSION }}-v ${{ github.event.inputs.tweak_version }}-${{ env.BUILD_DATE }}
          name: YouTubeLitePlusExtra_${{ env.YT_VERSION }}_v ${{ github.event.inputs.tweak_version }}_ ${{ env.BUILD_DATE }}
          files: YouTubeLitePlusExtra_${{ env.YT_VERSION }}_v ${{ github.event.inputs.tweak_version }}_${{ env.BUILD_DATE }}.ipa

      - name: Output Release URL
        run: |
          echo "::notice::Release available at: https://github.com/${{ github.repository }}/releases"